setTimeout(() => {
  const nfm = document.getElementById('no-file-message');
  const btn = document.getElementById('change');

  // 👇️ removes element from DOM
  nfm.style.display = 'none';

}, 5000); // 👈️ time in milliseconds